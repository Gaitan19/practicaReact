/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["dummyimage.com", "lh3.googleusercontent.com", "s.gravatar.com"],
  },
};

module.exports = nextConfig;
